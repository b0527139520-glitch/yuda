# הנחיות עבודה במאגר הזה

## פרסום ונראות

**המאגר ציבורי.** כל push גלוי לכל אדם תוך שניות, בכל ענף, כולל ההיסטוריה
המלאה. הענף שמשרת את GitHub Pages עולה לאתר החי תוך דקה-שתיים.

**דוחפים מיד, בלי לשאול.** זו החלטה מפורשת של בעל המאגר, שנמסרה לו לגביה
מלוא התמונה. אין צורך לבקש אישור לפני push של סיפור או ספר חדש.

הכתובת החיה: https://b0527139520-glitch.github.io/yuda/

## דיווח על Artifact

כשמפרסמים Artifact, מדווחים בפורמט הזה בדיוק:

```
**הקובץ פורסם כ-Artifact פרטי:**
<הקישור>
```

## ספרי HTML עם כמה סיפורים

כל ספר חדש שמכיל יותר מסיפור אחד **חייב** לכלול מערכת קישורים ישירים:

- `id` קבוע, ייחודי ומספרי לכל `<article>` — בפורמט `story-133`. לעולם לא
  מזהה שנגזר מכותרת שעשויה להשתנות.
- קישור מתוכן העניינים לכל סיפור (`href="#story-133"`).
- כפתור "העתק קישור" בכותרת כל סיפור וגם ליד כל שורה בתוכן העניינים.
- פתיחה ישירה דרך URL, עם הדגשה זמנית של הסיפור שנפתח.
- כפתור חזרה לתוכן העניינים (`id="toc"`) וניווט קודם/הבא.
- בדיקת תקינות לפני מסירה: אין `id` כפול, כל קישור מפנה ליעד קיים, וכל
  סיפור מופיע גם בגוף העמוד וגם ב-`searchData`.

### שתי מלכודות שכבר נתקלנו בהן

**כתובת לשיתוף בתוך iframe.** `location.href` מחזיר את כתובת ה-iframe
הפנימית כשהעמוד מוצג בתוך צופה — כתובת שאי אפשר לשתף. יש להעדיף את
`document.referrer` כשהעמוד ממוסגר.

**גלילה חלקה נלחמת בעוגן.** כשהעמוד נפתח עם `#story-N`, הדפדפן מתחיל
אנימציית גלילה משלו; התערבות באמצע גורמת לנחיתה עשרות עד אלפי פיקסלים
מהמקום. יש לכבות `scroll-behavior` לפני שהאנימציה מתחילה, ולהחזיר אותה
אחרי הנחיתה כדי שקליקים בתוך העמוד יישארו חלקים.

## מבנה קובץ: Artifact מול אחסון חיצוני

שני היעדים דורשים מבנה שונה, ואסור לבלבל ביניהם:

- **ל-Artifact** — תוכן עמוד בלבד. בלי `<!DOCTYPE>`, `<html>`, `<head>` או
  `<body>`; המערכת עוטפת בעצמה. את `dir="rtl"` ואת `lang` מגדירים מ-CSS
  ומסקריפט קצר.
- **לאחסון חיצוני (Pages, Drive, Sites)** — מסמך שלם ותקין.

בשני המקרים **חובה** להשאיר `<meta charset="UTF-8">` בראש הקובץ. בלעדיו
העברית נקראת כ-latin-1 והחיפוש מפסיק להתאים.

## מגבלות שכדאי לזכור

- קובץ בודד מעל 100MB — GitHub דוחה את ה-push.
- אתר GitHub Pages מוגבל ל-1GB. טקסט ו-HTML זניחים; אודיו ווידאו הם
  האילוץ האמיתי.
- קבצי Git LFS **אינם** מוגשים על ידי Pages.
